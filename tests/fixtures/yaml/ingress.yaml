apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: engines
spec:
  rules:
    - host: example.test
      http:
        paths:
          - path: /engines
            pathType: Prefix
          - path: /ingest
            pathType: Exact
